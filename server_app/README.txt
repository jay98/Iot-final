This code needs to be deployed to googel cloud ideally but you can also run this locally and it
should work.


The door_model.joblib is the model used to classify opening and closing of door

To install prequisits enter pip install -r requirments.txt

Before running the application config varaible need to be filled in according to your firebase config

Then to run just enter python main.py


